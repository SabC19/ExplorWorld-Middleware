# Establecer la propiedad principal para que Log4j2 encuentre la configuración
status = warn
name = ConfigSetlisto

# Definir los Appenders
appenders = console, file

# Configuración del Appender de Consola
appender.console.type = Console
appender.console.name = STDOUT
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n

# Configuración del Appender de Fichero
appender.file.type = File
appender.file.name = FileLogger
appender.file.fileName = setlisto.log
appender.file.layout.type = PatternLayout
appender.file.layout.pattern = %d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n

# Configuración del Root Logger (Afecta a todas las clases)
rootLogger.level = warn
rootLogger.appenderRefs = stdout, file
rootLogger.appenderRef.stdout.ref = STDOUT
rootLogger.appenderRef.file.ref = FileLogger

#Orden de los loggers (trace, debug, info, warn, error, fatal)